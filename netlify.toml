[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

# Official Next.js runtime — handles App Router, SSG, and route handlers on Netlify.
[[plugins]]
  package = "@netlify/plugin-nextjs"
